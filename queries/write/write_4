// Modify the properties of an edge

g.V().has("id", id).has("type", type)
	.outE(TYPE)
	.where(otherV().has("id", id).has("type", type))
	.property("p0", val0)
	.property("p1", val1)
	.property("p2", val2)
	.property("p3", val3)
